<html lang="en">
  <head>

    <!--
      This is the main Handlebars template for the site
      - When the user visits the homepage or submits a color the app calls the endpoints in server.js
      - The server script passes data in here and the Handlebars code builds it into the HTML page
    -->

    <meta charset="utf-8" />
    <link
      rel="icon"
      href="https://cdn-icons-png.flaticon.com/512/3274/3274099.png"
    />
    <meta name="viewport" content="width=device-width, initial-scale=1" />

    <title>Family Table</title>

    <!-- Import the webpage's stylesheet -->
    <link rel="stylesheet" href="/style.css" />
  </head>
  <body>
    <div class="content" role="main">
      <div class="hero">
        <h1 class="title">Family Table</h1>
        <div class="hero-description">
          Discover Delicious Home-Cooked Meals Near You
        </div>
      </div>
      <div class="family-table-instructions">
        <b>How to Order: </b>
        Each chef prepares a different meal everyday. Choose a chef to order
        from, and pick up your delicious meal at our designated location.
      </div>
      <div class="meal-container">
        <div class="order-header">
          <span class="order-for">Order for</span>
          <span>{{date}}</span>
        </div>
        <div class="meal-grid">
          {{#each meals.meals}}
            <a href="/meal?id={{@index}}">
              <div class="meal">
                <div class="meal-image">
                  <img src="{{image_url}}" />
                </div>
                <div class="meal-description">
                  <div class="chef">Chef {{cook}}</div>
                  <div class="category">{{category}}</div>
                  <div class="rating">★ {{rating}}</div>
                </div>
              </div>
            </a>
          {{/each}}
        </div>
      </div>
    </div>
  </body>
</html>
